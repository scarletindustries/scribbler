# P7-09 JS-corpus toolchain pin (mirrors test/carder/conformance/vendor/PIN).
#
# The vendored .wasm (Porffor's compiled output) and the baked .expected (`porf run` stdout,
# cross-checked against Node) are only trustworthy against a KNOWN toolchain — the exact ANSI
# color bytes and number rendering are Porffor-version-specific (T11/T13). Bumping either version
# is a REVIEWED change: re-run test/carder/js/vendor.sh to regenerate every .wasm + .expected and
# re-confirm the porf-vs-node categorization, then update corpus.gleam's measured note if the
# divergence set moves.
PORFFOR_VERSION=0.61.13
NODE_MAJOR=22
